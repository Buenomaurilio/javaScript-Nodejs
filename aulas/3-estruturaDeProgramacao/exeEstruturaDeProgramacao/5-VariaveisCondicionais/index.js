let velocidade = 90
let limite = 80

if(velocidade > limite) {
    console.log(`Você esta acima de ${limite}, recebeu uma multa.`)
} else {
    console.log('Você esta abaixo da velocidade permitida.')
}